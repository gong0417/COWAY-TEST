# Optional: API origin when frontend and backend are on different hosts (e.g. NAS).
# Leave empty for local dev (Vite proxies /api → backend).
VITE_API_BASE_URL=

# Google AI Studio → API 키 (요약 등)
VITE_GEMINI_API_KEY=

# Backend (Node)
# PORT=4000
# DATA_DIR=C:\path\to\RV-SYSTEM\data
